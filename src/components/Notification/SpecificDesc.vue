<template>
  <component-desc>
    <p>
      除了可以通过调用 <code>Notify(options)</code> 来呼出通知栏。 我也提前定义了多个 <code>type</code> 的单独调用方法，如:
      <code>Notify.success(options)</code>
    </p>
  </component-desc>
</template>

<script lang='ts' setup>
import ComponentDesc from '../common/ComponentDesc.vue';
</script>

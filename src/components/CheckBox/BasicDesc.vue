<template>
  <component-desc>
    <p>
      <code>checkbox-group</code>元素能把多个 checkbox 管理为一组，只需要在 Group 中使用 <code>v-model</code> 绑定 <code>Array</code>
      类型的变量即可。 只有一个选项时的默认值类型为 <code>Boolean</code>，当选中时值为
      <code>true</code>。
      <code>z-checkbox</code> 标签中的内容将成为复选框按钮之后的描述。
    </p>
  </component-desc>
</template>

<script lang='ts' setup>
import ComponentDesc from '../common/ComponentDesc.vue';
</script>

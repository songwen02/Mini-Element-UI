<demo>基础用法</demo>
<template>
  <component-desc>
    <p>
      <code>arrow</code> 属性定义了切换箭头的显示时机。 默认情况下，切换箭头只有在鼠标 <code>hover</code> 到走马灯上时才会显示。 若将 <code>arrow</code> 设置为 <code>always</code>，则会一直显示；设置为
      <code>never</code>，则会一直隐藏。
    </p>
  </component-desc>
</template>

<script lang='ts' setup>
import ComponentDesc from '../common/ComponentDesc.vue';
</script>
